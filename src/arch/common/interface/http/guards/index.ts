// Barrel export des guards communs (rate limiting, permissions, auth optionnel)
export { OptionalAuthGuard } from './optional-auth.guard';
export { PermissionsGuard } from './permissions.guard';
export { RateLimitGuard } from './rate-limit.guard';
